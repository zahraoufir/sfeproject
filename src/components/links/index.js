module.exports = class Controller extends Core.Controller{
    constructor(){
        super('links');
    }

/*     async create(req) {
        // req.body.firstname = 'Override';
        return super.create(req);
    } */
    
}